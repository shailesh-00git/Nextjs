# server actions:

Two ways to define server methods:
1.inline

- in the same file => create a function and inside it use flag "use server"

2. seperate

- make a seperate action file
  and inside it use flag " use server" and write code
