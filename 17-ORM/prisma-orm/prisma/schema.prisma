generator client {
  provider = "prisma-client-js"        // ✅ was "prisma-client" (missing -js)
  output   = "../generated/prisma"     // ✅ match your import "@/generated/prisma/client"
}

datasource db {
  provider = "postgresql"
}